# ScannR-Termux
ScanNR is a lightweight security scanner for Termux and Linux. It analyzes suspicious links, scans ports, performs geolocation and DNS lookups, and offers a full scan mode. Requires Python 3 and the requests &amp; colorama libraries.
